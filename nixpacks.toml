# Force Node.js — repo includes C# sample projects under assets/ that otherwise trigger dotnet
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = ["cd server && npm install"]

[start]
cmd = "cd server && node server.js"
